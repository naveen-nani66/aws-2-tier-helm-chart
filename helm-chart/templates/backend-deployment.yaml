apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backend.deploymentname }}
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      # CRITICAL: This allows K8s to pull from your private ECR
      imagePullSecrets:
        - name: ecr-registry-helper
      containers:
        - name: backend
          image: {{ .Values.backend.image }}
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              value: "my-rds-instance.c0vewi2umlii.us-east-1.rds.amazonaws.com"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.db.secrets.name }}
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.db.secrets.name }}
                  key: password
            - name: DB_NAME
              value: "appdb"
